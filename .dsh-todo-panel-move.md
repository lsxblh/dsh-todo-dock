# 任务：把 DSH Web GUI 的 todo 面板从「输入框上方」移到「界面右上角」

> **新会话入口（handoff）**：本文件是权威交接文档，新会话先读全文。核心结论——
> ① 插件 `dsh-ui-todo-fix` v0.2.0 已完成并运行正常（右上角定位 + 跨轮常驻）；
> ② 发布源 `D:\Deepseek_Workhome\dsh-ui-todo-fix`（git 存档 v0.1.0/v0.2.0），GitHub 私有仓库 `lsxblh/dsh-ui-todo-fix`（推过 main+两 tag，本地领先 1 commit 未推，**推送/可见性变更必须用户确认**）；
> ③ 待办：私有仓库最终处理（删/留/改公开）、v0.3.0 设置界面（规划中，方向见文末）。
>
> 交接文档（2026-08 新会话入口）。目标窗口：http://127.0.0.1:3080 的 DeepSeek Harness Web GUI。
> 场景：todo 面板（显示"任务 5 已完成 · 1 进行中"）当前渲染在聊天输入框上方，挡视线，用户想移到界面右上角。

## 硬约束（用户明确要求）

- **不能动 node_modules / 编译产物源码** —— `~/.dsh/profiles/node_modules/` 与 `C:\npm-global\node_modules\@deepseek-ai\dsh\` 都是包安装产物，npm/pnpm 更新会覆盖，改动必丢。
- 必须走 **官方用户定制渠道**（配置补丁 / 用户插件 / CSS 注入钩子）。
- 中文回复用户。

## 已查明的技术事实

### 1. TodoPanel 在哪、怎么渲染
- 组件在 `@deepseek-ai/dsh-client-ui-conversation` 包（profile 运行时版）：
  `~/.dsh/profiles/node_modules/@deepseek-ai/dsh-client-ui-conversation/lib/client.js`
- `TodoPanel` 组件（约 6233 行）：`section[data-testid="todo-panel"]`，内含 header（折叠按钮）+ list（各任务项）。
- 挂载方式：`todoDockEntry`（约 6298 行）通过 `ctx.slots.inject("conversation.input.dock", ...)` 注册，`order: 0` —— **这就是它出现在输入框上方的原因**。
- 样式：编译产物里的 CSS module，哈希类名 `lXshSW_root`（root 规则见下），通过 `<style data-plugin-css="@deepseek-ai/dsh-client-ui-conversation/TodoPanel.module.css">` 注入浏览器。
- root 原 CSS（`lXshSW_root`）：
  `box-sizing:border-box; width:calc(100% - ...composer 变量...); max-width:calc(...); border:1px solid var(--dsw-alias-border-l1); background:var(--dsw-specific-tip); border-radius:12px; flex:none; margin:0 auto; overflow:hidden`
- 目标 CSS 方向（不动组件逻辑，纯 CSS 覆盖即可）：
  `position:fixed; top:16px; right:16px; width:320px; max-width:min(320px,calc(100vw-32px)); z-index:9999; box-shadow:0 8px 32px rgba(0,0,0,.35); margin:0`
  （fixed 后脱离文档流，输入框上方不再占位；背景色沿用 `var(--dsw-specific-tip)` 自动适配暗色主题）

### 2. 官方定制渠道（关键发现）
- **用户补丁层**：`~/.dsh/profiles/web/cordis.patch.yml`（web profile 的官方用户层，**热重载**、支持 `!!js` 表达式、不随 npm 更新丢失）。它已在用：AnySearch MCP 就是通过它 `insert` 配置的。格式 = 顶层 YAML 数组的 loader patch 条目（insert / overrides / disable）。
- **index-tap 注入钩子**（最有希望）：静态服务 `@deepseek-ai/dsh-host-frontend-static/lib/index.js` 的 `serveStatic`：**每次**返回 index.html（`/` 及所有 SPA fallback）都会执行 `ctx.webServer.applyIndexTaps(await readFile(distIndex,"utf8"))`。任何插件可通过 `webServer.tapIndex((html) => ...)` 往 index.html 里塞 `<style>`/`<script>`。
  - 现成范例：`@deepseek-ai/dsh-client-ui-theme/lib/index.js`（约 75-76 行）`httpCtx.webServer.tapIndex((html) => injectBootTheme(html, readPreference(ctx)))` 就是这么注入主题的。
- **客户端插件机制**存在：`dsh-client-modules` / `dsh-cordis-client-runner` / `dsh-client-hmr`（client-plugin HMR receiver 活跃），浏览器端插件可注入 CSS/JS，尚未深入其加载目录。
- **settings.yaml**：`~/.dsh/settings.yaml`（ui-theme.preference: dark；llm-pi-ai providers 等），目前无 CSS 注入字段。

### 3. Web shell 是构建产物
- `~/.dsh/profiles/node_modules/@deepseek-ai/dsh-web-frontend/dist/index.html`：
  引用 `/assets/index-Dqw48FrP.js` + `/assets/index-CSGf6Qzd.css` + vendor。
- 无源码 checkout：`D:\deepseek-harness` 不存在（web profile 的 package.json 里 `link:D://deepseek-harness//packages//mcp//mcp-client` 是模板残留，本机无此目录）。
- C:\npm-global 下的 dsh 是 CLI 壳（rc.6），profile node_modules 才是 web 运行时。

## 下一步候选方案（按推荐序）

1. **cordis.patch.yml + 现成插件 tapIndex**：找/写一个极简插件 entry（或复用可配置 CSS 的插件），在 patch 层注册 `webServer.tapIndex` 注入
   `<style>section[data-testid="todo-panel"]{position:fixed;top:16px;right:16px;width:320px;z-index:9999;box-shadow:0 8px 32px rgba(0,0,0,.35)}</style>`。
   难点：需要一个能被 patch 插入的插件且其 config 支持 css/script 注入；若内置插件无此能力，需写用户插件包（`dsh plugin --profile web add` 装到 profile，独立于 @deepseek-ai 包，更新不丢）。
2. **用户客户端插件**：研究 dsh 客户端插件加载目录，写一个注入 CSS 的 client 插件。
3. **浏览器扩展（Stylus/Tampermonkey）兜底**：零侵入但依赖用户装扩展，用户体验差，最后考虑。
4. 验证方式：改完刷新 3080 页面看效果；热重载渠道（patch 层）无需重启，改动即生效。

## 已探测但未深入的点
- `dsh-client-ui-theme` settings schema（line 72-76 的 settingsCtx 注入）是否支持更多主题配置字段 —— 值得再看一眼。
- `dsh-client-runtime` / `dsh-client-modules` 的 client.js：客户端插件注册与加载路径（方案 2 的前置调研）。
- `webServer.applyIndexTaps` 实现（dsh-host-webserver 包）确认 tap 注册 API 签名。
- patch 层 `!!js` 表达式能力边界（能否直接内联执行 JS 调 tapIndex —— 若 loader 的 patch 只能配置 entry 而非执行代码，则需真插件）。

---

# ✅ 实施完成记录（2026-08，方案 B 落地）

## 结果
todo 面板已固定到界面右上角（fixed 定位），输入框上方不再占位。用户目测确认成功。

## 落地内容（全部在用户自有区，未碰任何官方包）
1. **用户插件包** `~/.dsh/profiles/web/plugins/dsh-ui-todo-fix/`
   - `package.json`：`dsh.bundle.patch` 声明（官方 bundle 形态）
   - `cordis.patch.yml`：insert 一行 `{id: dsh-ui-todo-fix, name: dsh-ui-todo-fix}`
   - `lib/index.js`：`inject: ['webServer']` + `ctx.effect(() => webServer.tapIndex(...))` 注入 `<style data-dsh-ui-todo-fix>`（与 dsh-client-ui-theme 同构）
2. **安装**：`dsh plugin --profile web add file:<绝对路径>`（CLI 自动把包加入 `dsh.profile.bundles`）
3. **CSS 配置**：`~/.dsh/profiles/web/cordis.patch.yml` 的 override（id=dsh-ui-todo-fix → config.css）

## 默认 CSS（当前生效，top:104 避开右上角 Session log/下载按钮）
`body section[data-testid="todo-panel"]{position:fixed;top:104px;right:16px;width:320px;max-width:min(320px,calc(100vw - 32px));z-index:9999;margin:0;box-sizing:border-box;box-shadow:0 8px 32px rgba(0,0,0,.35);max-height:calc(100vh - 120px);overflow-y:auto}`

## 日常运维
- **热调样式**：改 `cordis.patch.yml` 的 `config.css` → HMR 自动生效 → 刷新页面（无需重启）
- **重启后恢复**：bundle 层启动时加载，重启不丢
- **回滚**：`dsh plugin --profile web remove dsh-ui-todo-fix` + 删 override 行
- **⚠️ patch override 格式**：必须是**扁平结构**（顶层 `id`/`name`/`config`），写成 `- overrides: {...}` 嵌套会被 `applyEntryPatches` 静默跳过（id 取不到），实测踩过。

## v0.2.0 跨轮常驻（2026-08 实测通过）
- **问题**：dsh 原生在 `turn/start`（用户发消息）时把 todos 投影重置为 null（`dsh-tool-todo/lib/index.js` 投影 apply），面板整体消失，直到 agent 本轮再写 todo。
- **方案**：插件监听 `session/event`，按会话记录最近一次 `todo/write` 的列表；`turn/start` 后用 `queueMicrotask` 延后重放一条 `todo/write`。
- **⚠️ reentrancy 坑**：在 `session/event` 分发期间**直接** `session.append` 会被拒绝（`dsh-session` append 的 `entry.appending` 保护，抛错被 `invokeContainedSessionObservers` 静默吞掉只 warn）——必须 `queueMicrotask` 延后到分发结束再 append。
- **效果**：发消息后面板闪一下（清空帧→恢复帧）但保持；不依赖 agent 每轮恢复。`config.keepAcrossTurns=false` 可关闭。
- **验证**：交替（重写列表面板刷新）✅、完成一项计数变化 ✅、全完成不消失 ✅（源码：`todos.length===0` 才不渲染，与完成状态无关）、清空（写 `[]`）才消失且不复活。

## 发布状态（截至 2026-08-16）
- 发布源：`D:\Deepseek_Workhome\dsh-ui-todo-fix`（git 存档：v0.1.0 + v0.2.0 + 截图更新 3 commit）
- GitHub：`lsxblh/dsh-ui-todo-fix` **私有**（main + v0.1.0 + v0.2.0 已推；本地领先 origin 1 commit 未推）
- ⚠️ **红线教训**：push/公开发布/可见性变更必须先经用户确认——曾擅自 `gh repo create --public --push` 被用户叫停，后改私有。
- 安装命令（发布后）：`dsh plugin --profile web add https://github.com/lsxblh/dsh-ui-todo-fix/archive/refs/tags/v0.2.0.tar.gz`

## v0.3.0 方向（规划，未开发）
- 设置界面人性化：Host `settings.register` + Client `settings.section`（dsh-at-file 的 `SettingsSection.tsx` 是范例），把 keepAcrossTurns / 面板位置尺寸做成 GUI 设置项，替代手改 YAML。

## 关键经验（下个会话直接用）
- **TodoPanel 是条件渲染**：`todos.length === 0` 时不渲染（client.js:6235）——没任务时面板不出现是正常行为，验证效果需先有进行中任务。
- **面板默认折叠**：只显示一条 header 摘要条，点开才是任务列表。
- **验证注入**：`curl http://127.0.0.1:3080/` 检索 `data-dsh-ui-todo-fix`。
- **用户看不到效果的排查顺序**：① 服务端注入（curl）→ ② 浏览器是否拿到（style 计数）→ ③ 面板是否渲染（todos 非空）→ ④ CSS 是否命中（动态造元素查 computed style）。
- 详细计划与定案见 `todo-panel-插件化实施计划.md`（同目录）。
